// array -> store multiple values in same variable
// array.length -> Total length of array

let marks = [400, 500, 300, 200, 100, 154, 475];

let numbers = [10, 9, 5, 4];

let fruits = ['Apple', 'Banana', 'Mango'];

for(let i = 0; i<marks.length; i++) {
    document.write(marks[i]);
    document.write('<br>');
}

// marks[0] -> return 1st value at index 0